import { Algo } from "../algo";

const description = 'This function takes a single argument of type number, adds 5, and returns the result';
const argArr = ['num']
const isEven = (num) => num % 2 === 0

// const IsEven = new Algo ('isEven', description, argArr, isEven)


// export { IsEven as default }